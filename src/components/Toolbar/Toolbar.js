import React from 'react';
import classes from './Toolbar.module.css';

const toolbar = () => (
  <header className={classes.Toolbar}>
    <h1>Reactive Productivity | Simple todo app</h1>
  </header>
);

export default toolbar;
